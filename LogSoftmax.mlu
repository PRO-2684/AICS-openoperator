#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 003 003_LogSoftmax
// Category: softmax
// Difficulty: basic
// DType: float16
// Description: 实现一个 BangC 算子，对输入张量在指定维度上执行 LogSoftmax 激活计算。输入包括形状为 `[batch_size, dim]` 的张量 `x` 以及用于指定计算维度的整数参数 `dim`；输出张量的形状与 `x` 保持一致。
// Wrapper: torch::Tensor bang_func(torch::Tensor x, int dim);

torch::Tensor bang_func(torch::Tensor x, int dim) {
  TORCH_CHECK(false, "TODO: implement LogSoftmax");
}
