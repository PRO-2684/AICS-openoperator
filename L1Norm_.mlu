#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 035 035_L1Norm_
// Category: reduce
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，对输入张量沿维度 1 执行 L1 归一化操作。输入为形状为 `[batch, dim]` 的张量 `x`；通过计算 `x` 在维度 1 上的绝对值之和对原张量进行缩放，输出与 `x` 形状相同。
// Wrapper: torch::Tensor bang_func(torch::Tensor x);

torch::Tensor bang_func(torch::Tensor x) {
  TORCH_CHECK(false, "TODO: implement L1Norm_");
}
