#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 036 036_L2Norm_
// Category: reduce
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，对输入张量在指定维度上进行 L2 归一化处理。输入为形状为 `[batch, dim]` 的张量 `x`，通过计算其在 `dim` 维度上的二范数并执行逐元素除法，使输出向量的模长为 1；输出张量与 `x` 形状相同。
// Wrapper: torch::Tensor bang_func(torch::Tensor x);

torch::Tensor bang_func(torch::Tensor x) {
  TORCH_CHECK(false, "TODO: implement L2Norm_");
}
