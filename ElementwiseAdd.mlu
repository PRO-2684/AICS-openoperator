#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 072 072_ElementwiseAdd
// Category: pointwise
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，执行两个二维张量的逐元素加法运算。输入包括形状均为 `[M, N]` 的张量 `a` 和 `b`；输出结果的形状与输入张量相同。
// Wrapper: torch::Tensor bang_func(torch::Tensor a, torch::Tensor b);

torch::Tensor bang_func(torch::Tensor a, torch::Tensor b) {
  TORCH_CHECK(false, "TODO: implement ElementwiseAdd");
}
