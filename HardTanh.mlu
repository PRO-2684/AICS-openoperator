#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 029 029_HardTanh
// Category: pointwise
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，对输入张量应用 HardTanh 激活函数，实现元素级的数值截断。输入包括形状为 `[batch_size, dim]` 的张量 `x`，以及用于指定裁剪范围的浮点标量参数 `min_val` 和 `max_val`；输出张量与 `x` 具有相同的形状。
// Wrapper: torch::Tensor bang_func(torch::Tensor x);

torch::Tensor bang_func(torch::Tensor x) {
  TORCH_CHECK(false, "TODO: implement HardTanh");
}
