#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 062 062_compute_agg
// Category: reduce
// Difficulty: medium
// DType: int32
// Description: 实现一个 BangC 算子，对一维输入张量执行包含当前位置的累加前缀和（Inclusive Prefix Sum）运算。输入为一个形状为 `[N]` 的 `int32` 类型张量 `agg`；输出为数据类型转换为 `int64` 后的前缀和计算结果，其形状与输入相同。
// Wrapper: torch::Tensor bang_func(torch::Tensor agg);

torch::Tensor bang_func(torch::Tensor agg) {
  TORCH_CHECK(false, "TODO: implement compute_agg");
}
