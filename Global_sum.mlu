#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 079 079_Global_sum
// Category: reduce
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，计算输入张量中所有元素的全局总和。输入为形状为 `[batch, dim]` 的张量 `x`；输出为包含所有元素累加结果的标量。
// Wrapper: torch::Tensor bang_func(torch::Tensor x);

torch::Tensor bang_func(torch::Tensor x) {
  TORCH_CHECK(false, "TODO: implement Global_sum");
}
