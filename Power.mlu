#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 073 073_Power
// Category: pointwise
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，计算输入张量各元素绝对值的指定幂次。输入包括形状为 `[batch, dim]` 的张量 `x` 以及一个表示指数的浮点数标量 `exponent`；输出张量的形状与 `x` 相同。
// Wrapper: torch::Tensor bang_func(torch::Tensor x, double exponent);

torch::Tensor bang_func(torch::Tensor x, double exponent) {
  TORCH_CHECK(false, "TODO: implement Power");
}
