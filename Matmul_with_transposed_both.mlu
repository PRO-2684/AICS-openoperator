#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 022 022_Matmul_with_transposed_both
// Category: matrix
// Difficulty: medium
// DType: float16
// Description: 实现一个 BangC 算子，对两个输入矩阵分别进行转置并计算其矩阵乘积。输入包括形状为 `[K, M]` 的张量 `A` 和形状为 `[N, K]` 的张量 `B`；输出为形状为 `[M, N]` 的结果张量。
// Wrapper: torch::Tensor bang_func(torch::Tensor A, torch::Tensor B);

torch::Tensor bang_func(torch::Tensor A, torch::Tensor B) {
  TORCH_CHECK(false, "TODO: implement Matmul_with_transposed_both");
}
