#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 077 077_LogSumExp_over_dim
// Category: reduce
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，沿指定维度对输入张量进行对数指数和（LogSumExp）计算。输入包括形状为 `[batch, dim]` 的张量 `x` 以及一个指定归约维度的整数参数 `dim_idx`；输出为沿该维度计算后的张量。
// Wrapper: torch::Tensor bang_func(torch::Tensor x, int dim);

torch::Tensor bang_func(torch::Tensor x, int dim) {
  TORCH_CHECK(false, "TODO: implement LogSumExp_over_dim");
}
