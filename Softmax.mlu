#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 058 058_Softmax
// Category: softmax
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，对输入的二维张量在特征维度（dim=1）上应用 Softmax 归一化处理。输入为形状为 `[batch_size, num_features]` 的张量 `x`；输出张量与 `x` 形状相同，表示计算后的概率分布。
// Wrapper: torch::Tensor bang_func(torch::Tensor x);

torch::Tensor bang_func(torch::Tensor x) {
  TORCH_CHECK(false, "TODO: implement Softmax");
}
