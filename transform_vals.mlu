#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 066 066_transform_vals
// Category: elementwise
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，实现两个张量之间的逐元素加法运算。输入包括形状均为 `[D1, D2, ...]` 的张量 `a` 和 `b`；输出张量与输入形状相同。
// Wrapper: torch::Tensor bang_func(torch::Tensor a, torch::Tensor b);

torch::Tensor bang_func(torch::Tensor a, torch::Tensor b) {
  TORCH_CHECK(false, "TODO: implement transform_vals");
}
