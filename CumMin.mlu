#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 078 078_CumMin
// Category: cum
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，计算输入张量沿指定维度的累积最小值。输入包括形状为 `[batch, length]` 的张量 `x` 和一个指定计算维度的整数 `dim`；输出为累积最小值张量，其形状与 `x` 保持一致。
// Wrapper: torch::Tensor bang_func(torch::Tensor x, int dim);

torch::Tensor bang_func(torch::Tensor x, int dim) {
  TORCH_CHECK(false, "TODO: implement CumMin");
}
