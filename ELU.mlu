#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 026 026_ELU
// Category: pointwise
// Difficulty: easy
// DType: float16
// Description: 实现一个 BangC 算子，对输入张量应用 ELU 激活函数处理。输入包括形状为 `[batch, dim]` 的张量 `x` 以及表示激活函数比例系数的浮点标量 `alpha`；输出结果张量与 `x` 的形状相同。
// Wrapper: torch::Tensor bang_func(torch::Tensor x, float alpha);

torch::Tensor bang_func(torch::Tensor x, float alpha) {
  TORCH_CHECK(false, "TODO: implement ELU");
}
