#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 042 042_FrobeniusNorm_
// Category: fused
// Difficulty: medium
// DType: float16
// Description: 实现一个 BangC 算子，对输入张量进行 Frobenius 范数归一化。输入为一个形状为 `[D1, D2, ..., Dn]` 的张量 `x`；输出为 `x` 除以其全局 Frobenius 范数（即所有元素平方和的平方根）后的结果，形状与输入一致。
// Wrapper: torch::Tensor bang_func(torch::Tensor x);

torch::Tensor bang_func(torch::Tensor x) {
  TORCH_CHECK(false, "TODO: implement FrobeniusNorm_");
}
