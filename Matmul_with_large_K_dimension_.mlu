#include <bang.h>
#include <cnrt.h>
#include <torch/extension.h>

// Task: 020 020_Matmul_with_large_K_dimension_
// Category: matrix
// Difficulty: medium
// DType: float16
// Description: 实现一个 BangC 算子，实现两个二维矩阵的乘法运算。输入包括形状为 `[M, K]` 的左矩阵 `A` 和形状为 `[K, N]` 的右矩阵 `B`；输出结果为形状为 `[M, N]` 的张量。
// Wrapper: torch::Tensor bang_func(torch::Tensor A, torch::Tensor B);

torch::Tensor bang_func(torch::Tensor A, torch::Tensor B) {
  TORCH_CHECK(false, "TODO: implement Matmul_with_large_K_dimension_");
}
