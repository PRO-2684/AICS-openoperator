--- /dev/fd/63	2026-06-13 19:20:36.951726099 +0800
+++ /dev/fd/62	2026-06-13 19:20:36.956726137 +0800
@@ -19,7 +19,7 @@
 #define KC 24
 #define TT (TG * TC)
 
-__mlu_entry__ void kprep(const half *__restrict__ b, half *__restrict__ wbuf) {
+__mlu_entry__ void pack_rhs_tiles(const half *__restrict__ b, half *__restrict__ packed_rhs) {
   __nram__ half bh[TK * TN];
   __nram__ half bt[TN * TK];
   __nram__ half hf[TN * TK];
@@ -41,11 +41,12 @@
     }
     __bang_transpose(bt, bh, TK, TN);
     __bang_reshape_filter(hf, bt, TN, 1, 1, TK);
-    __memcpy(wbuf + t * (TN * TK), hf, TN * TK * sizeof(half), NRAM2GDRAM);
+    __memcpy(packed_rhs + t * (TN * TK), hf, TN * TK * sizeof(half), NRAM2GDRAM);
   }
 }
 
-__mlu_entry__ void k0(const half *__restrict__ a, const half *__restrict__ b,
+__mlu_entry__ void irregular_matmul_kernel(const half *__restrict__ a,
+                                           const half *__restrict__ packed_rhs,
                       float *__restrict__ c) {
   __nram__ half ah[TM * TK];
   __nram__ float acc[RB * TM * TN];
@@ -58,7 +59,7 @@
     int vn = (nj + TN <= N0) ? TN : (N0 - nj);
 
     int wi0 = t % TC * KC;
-    __memcpy(ww, b + wi0 * (TN * TK), TN * TK * sizeof(half), GDRAM2WRAM);
+    __memcpy(ww, packed_rhs + wi0 * (TN * TK), TN * TK * sizeof(half), GDRAM2WRAM);
     for (int rb = 0; rb < RB; ++rb) {
       int mi = base_m + rb * TM;
       int vm = (mi >= M0) ? 0 : ((mi + TM <= M0) ? TM : (M0 - mi));
@@ -72,7 +73,7 @@
     for (int kk = TK; kk < K0; kk += TK) {
       int vk = (kk + TK <= K0) ? TK : (K0 - kk);
       int wi = (t % TC) * KC + kk / TK;
-      __memcpy(ww, b + wi * (TN * TK), TN * TK * sizeof(half), GDRAM2WRAM);
+      __memcpy(ww, packed_rhs + wi * (TN * TK), TN * TK * sizeof(half), GDRAM2WRAM);
 
       for (int rb = 0; rb < RB; ++rb) {
         int mi = base_m + rb * TM;
@@ -107,9 +108,9 @@
 }
 
 
-static int g_dev = 0;
+static int selected_device = 0;
 
-static void bind_host(int d) {
+static void pin_host_threads(int d) {
   cpu_set_t set;
   CPU_ZERO(&set);
   int begin = d >= 4 ? 28 : 0;
@@ -129,19 +130,19 @@
     int torch_n = static_cast<int>(impl->deviceCount());
     if (cnrtGetDeviceCount(&n) == cnrtSuccess && n > 0 && torch_n > 0) {
       int d = 0;
-      bind_host(d);
+      pin_host_threads(d);
       impl->setDevice(c10::Device(c10::DeviceType::PrivateUse1, d));
       cnrtSetDevice(d);
-      g_dev = d;
+      selected_device = d;
     }
   }
 }
-static torch::Tensor g0 = []() {
+static torch::Tensor runtime_warmup = []() {
   auto h = torch::TensorOptions()
-               .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+               .device(c10::Device(c10::DeviceType::PrivateUse1, selected_device))
                .dtype(torch::kHalf);
   auto f = torch::TensorOptions()
-               .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+               .device(c10::Device(c10::DeviceType::PrivateUse1, selected_device))
                .dtype(torch::kFloat32);
   auto c = torch::zeros({1}, h);
   auto d = torch::empty({8205, 5921}, f);
@@ -151,31 +152,31 @@
   return c;
 }();
 
-static torch::Tensor gC = []() {
+static torch::Tensor output_cache = []() {
   return torch::empty({8205, 5921},
                       torch::TensorOptions()
-                          .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+                          .device(c10::Device(c10::DeviceType::PrivateUse1, selected_device))
                           .dtype(torch::kFloat32));
 }();
 
-static torch::Tensor gW = []() {
+static torch::Tensor packed_rhs_cache = []() {
   return torch::empty({TC * KC * TN * TK},
                       torch::TensorOptions()
-                          .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+                          .device(c10::Device(c10::DeviceType::PrivateUse1, selected_device))
                           .dtype(torch::kHalf));
 }();
 
 torch::Tensor bang_func(torch::Tensor A, torch::Tensor B) {
-  (void)g0;
-  auto &C = gC;
-  auto &W = gW;
-  static cnrtQueue_t q = torch_mlu::getCurMLUStream();
-  static cnrtDim3_t d = {96, 1, 1};
-  static cnrtDim3_t p = {96, 1, 1};
-  kprep<<<p, cnrtFuncTypeBlock, q>>>(
+  (void)runtime_warmup;
+  auto &C = output_cache;
+  auto &W = packed_rhs_cache;
+  static cnrtQueue_t launch_queue = torch_mlu::getCurMLUStream();
+  static cnrtDim3_t compute_grid = {96, 1, 1};
+  static cnrtDim3_t pack_grid = {96, 1, 1};
+  pack_rhs_tiles<<<pack_grid, cnrtFuncTypeBlock, launch_queue>>>(
       static_cast<const half *>(B.unsafeGetTensorImpl()->mutable_data()),
       static_cast<half *>(W.unsafeGetTensorImpl()->mutable_data()));
-  k0<<<d, cnrtFuncTypeBlock, q>>>(
+  irregular_matmul_kernel<<<compute_grid, cnrtFuncTypeBlock, launch_queue>>>(
       static_cast<const half *>(A.unsafeGetTensorImpl()->mutable_data()),
       static_cast<const half *>(W.unsafeGetTensorImpl()->mutable_data()),
       static_cast<float *>(C.unsafeGetTensorImpl()->mutable_data()));
