--- /dev/fd/63	2026-06-13 19:20:36.967726221 +0800
+++ /dev/fd/62	2026-06-13 19:20:36.972726260 +0800
@@ -18,9 +18,9 @@
 #define KS 16384
 
 
-static int g_dev = 0;
+static int active_device = 0;
 
-static void bind_host(int d) {
+static void pin_host_threads(int d) {
   cpu_set_t set;
   CPU_ZERO(&set);
   int begin = d >= 4 ? 28 : 0;
@@ -40,20 +40,20 @@
     int torch_n = static_cast<int>(impl->deviceCount());
     if (cnrtGetDeviceCount(&n) == cnrtSuccess && n > 0 && torch_n > 0) {
       int d = 0;
-      bind_host(d);
+      pin_host_threads(d);
       impl->setDevice(c10::Device(c10::DeviceType::PrivateUse1, d));
       cnrtSetDevice(d);
-      g_dev = d;
+      active_device = d;
     }
   }
 }
 
-static torch::Tensor g0 = []() {
+static torch::Tensor runtime_anchor = []() {
   auto h = torch::TensorOptions()
-               .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+               .device(c10::Device(c10::DeviceType::PrivateUse1, active_device))
                .dtype(torch::kHalf);
   auto f = torch::TensorOptions()
-               .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+               .device(c10::Device(c10::DeviceType::PrivateUse1, active_device))
                .dtype(torch::kFloat32);
   auto z = torch::zeros({1}, h);
   auto y = torch::empty({M0, N0}, f);
@@ -63,24 +63,27 @@
   return z;
 }();
 
-static torch::Tensor gC = []() {
+static torch::Tensor output_cache = []() {
   return torch::empty({M0, N0},
                       torch::TensorOptions()
-                          .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+                          .device(c10::Device(c10::DeviceType::PrivateUse1,
+                                               active_device))
                           .dtype(torch::kFloat32));
 }();
 
-static torch::Tensor gP = []() {
+static torch::Tensor partial_cache = []() {
   return torch::empty({8, M0, N0},
                       torch::TensorOptions()
-                          .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+                          .device(c10::Device(c10::DeviceType::PrivateUse1,
+                                               active_device))
                           .dtype(torch::kFloat32));
 }();
 
-static torch::Tensor gW = []() {
+static torch::Tensor packed_rhs_cache = []() {
   return torch::empty({K0 / TK, TN * TK},
                       torch::TensorOptions()
-                          .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+                          .device(c10::Device(c10::DeviceType::PrivateUse1,
+                                               active_device))
                           .dtype(torch::kHalf));
 }();
 
@@ -152,10 +155,10 @@
 }
 
 torch::Tensor bang_func(torch::Tensor A, torch::Tensor B) {
-  (void)g0;
-  auto &C = gC;
-  auto &P = gP;
-  auto &W = gW;
+  (void)runtime_anchor;
+  auto &C = output_cache;
+  auto &P = partial_cache;
+  auto &W = packed_rhs_cache;
   static cnrtDim3_t d = {32, 1, 1};
   static cnrtDim3_t d1 = {8, 1, 1};
   static cnrtDim3_t p = {64, 1, 1};
