--- /dev/fd/63	2026-06-13 19:20:36.935725976 +0800
+++ /dev/fd/62	2026-06-13 19:20:36.939726007 +0800
@@ -11,9 +11,9 @@
 #define Q 512
 
 
-static int g_dev = 0;
+static int selected_device = 0;
 
-static void bind_host(int d) {
+static void pin_host_threads(int d) {
   cpu_set_t set;
   CPU_ZERO(&set);
   int begin = d >= 4 ? 28 : 0;
@@ -33,44 +33,45 @@
     int torch_n = static_cast<int>(impl->deviceCount());
     if (cnrtGetDeviceCount(&n) == cnrtSuccess && n > 0 && torch_n > 0) {
       int d = 0;
-      bind_host(d);
+      pin_host_threads(d);
       impl->setDevice(c10::Device(c10::DeviceType::PrivateUse1, d));
       cnrtSetDevice(d);
-      g_dev = d;
+      selected_device = d;
     }
   }
 }
-static torch::Tensor g0 = []() {
+static torch::Tensor runtime_warmup = []() {
   auto opt = torch::TensorOptions()
-                 .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+                 .device(c10::Device(c10::DeviceType::PrivateUse1, selected_device))
                  .dtype(torch::kHalf);
   return torch::zeros({1}, opt);
 }();
 
-static torch::Tensor gy = []() {
+static torch::Tensor output_cache = []() {
   return torch::empty(
       {4, 64, 64, 64},
       torch::TensorOptions()
-          .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+          .device(c10::Device(c10::DeviceType::PrivateUse1, selected_device))
           .dtype(torch::kHalf));
 }();
 
-static torch::Tensor go = []() {
+static torch::Tensor grid_offset_cache = []() {
   return torch::empty(
       {4, P * 4}, torch::TensorOptions()
-                      .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+                      .device(c10::Device(c10::DeviceType::PrivateUse1, selected_device))
                       .dtype(torch::kInt32));
 }();
 
-static torch::Tensor gc = []() {
+static torch::Tensor grid_weight_cache = []() {
   return torch::empty(
       {4, P * 4}, torch::TensorOptions()
-                      .device(c10::Device(c10::DeviceType::PrivateUse1, g_dev))
+                      .device(c10::Device(c10::DeviceType::PrivateUse1, selected_device))
                       .dtype(torch::kHalf));
 }();
 
-__mlu_entry__ void kprep(const half *__restrict__ g, int *__restrict__ off,
-                         half *__restrict__ cf) {
+__mlu_entry__ void prepare_grid_lookup(const half *__restrict__ grid,
+                                       int *__restrict__ offsets,
+                                       half *__restrict__ weights) {
   __nram__ half gg[Q * 2];
   __nram__ half ch0[Q];
   __nram__ half ch1[Q];
@@ -82,9 +83,9 @@
   __nram__ int o3[Q];
   int n = taskId >> 3;
   int t = (taskId & 7) * Q;
-  const half *pg = g + n * 8192;
-  int *po = off + n * P * 4;
-  half *pc = cf + n * P * 4;
+  const half *pg = grid + n * 8192;
+  int *po = offsets + n * P * 4;
+  half *pc = weights + n * P * 4;
   {
     __memcpy(gg, pg + t * 2, Q * 2 * sizeof(half), GDRAM2NRAM);
 #pragma unroll 1
@@ -117,8 +118,10 @@
   }
 }
 
-__mlu_entry__ void k0(const half *__restrict__ x, const int *__restrict__ off,
-                      const half *__restrict__ cf, half *__restrict__ y) {
+__mlu_entry__ void apply_grid_sample(const half *__restrict__ input,
+                                     const int *__restrict__ offsets,
+                                     const half *__restrict__ weights,
+                                     half *__restrict__ output) {
   __nram__ int o0[T];
   __nram__ int o1[T];
   __nram__ int o2[T];
@@ -135,8 +138,8 @@
   for (int gc = taskId; gc < 128; gc += taskDim) {
     int n = gc >> 5;
     int cb = (gc & 31) << 1;
-    const int *po = off + n * P * 4;
-    const half *pc = cf + n * P * 4;
+    const int *po = offsets + n * P * 4;
+    const half *pc = weights + n * P * 4;
     __memcpy(o0, po, T * sizeof(int), GDRAM2NRAM);
     __memcpy(o1, po + P, T * sizeof(int), GDRAM2NRAM);
     __memcpy(o2, po + P * 2, T * sizeof(int), GDRAM2NRAM);
@@ -147,8 +150,8 @@
     __memcpy(c3h, pc + P * 3, T * sizeof(half), GDRAM2NRAM);
     for (int j = 0; j < 2; ++j) {
       int c = cb + j;
-      const half *px = x + n * 1048576 + c * 16384;
-      half *py = y + n * 262144 + c * 4096;
+      const half *px = input + n * 1048576 + c * 16384;
+      half *py = output + n * 262144 + c * 4096;
       __gather(v0h, px, (uint32_t *)o0, sizeof(half), GDRAM2NRAM,
                sizeof(half), T);
       __gather(v1h, px, (uint32_t *)o1, sizeof(half), GDRAM2NRAM,
@@ -246,20 +249,20 @@
   }
 }
 
-static cnrtQueue_t queue = torch_mlu::getCurMLUStream();
-static cnrtDim3_t dim0 = {128, 1, 1};
+static cnrtQueue_t launch_queue = torch_mlu::getCurMLUStream();
+static cnrtDim3_t sample_grid = {128, 1, 1};
 
 torch::Tensor bang_func(torch::Tensor input, torch::Tensor grid) {
-  (void)g0;
-  cnrtDim3_t dprep = {32, 1, 1};
-  kprep<<<dprep, cnrtFuncTypeBlock, queue>>>(
+  (void)runtime_warmup;
+  cnrtDim3_t lookup_grid = {32, 1, 1};
+  prepare_grid_lookup<<<lookup_grid, cnrtFuncTypeBlock, launch_queue>>>(
       static_cast<const half *>(grid.unsafeGetTensorImpl()->mutable_data()),
-      static_cast<int *>(go.unsafeGetTensorImpl()->mutable_data()),
-      static_cast<half *>(gc.unsafeGetTensorImpl()->mutable_data()));
-  k0<<<dim0, cnrtFuncTypeBlock, queue>>>(
+      static_cast<int *>(grid_offset_cache.unsafeGetTensorImpl()->mutable_data()),
+      static_cast<half *>(grid_weight_cache.unsafeGetTensorImpl()->mutable_data()));
+  apply_grid_sample<<<sample_grid, cnrtFuncTypeBlock, launch_queue>>>(
       static_cast<const half *>(input.unsafeGetTensorImpl()->mutable_data()),
-      static_cast<const int *>(go.unsafeGetTensorImpl()->mutable_data()),
-      static_cast<const half *>(gc.unsafeGetTensorImpl()->mutable_data()),
-      static_cast<half *>(gy.unsafeGetTensorImpl()->mutable_data()));
-  return gy;
+      static_cast<const int *>(grid_offset_cache.unsafeGetTensorImpl()->mutable_data()),
+      static_cast<const half *>(grid_weight_cache.unsafeGetTensorImpl()->mutable_data()),
+      static_cast<half *>(output_cache.unsafeGetTensorImpl()->mutable_data()));
+  return output_cache;
 }
